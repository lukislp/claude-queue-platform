# Placeholder - generate the real SealedSecret before the first Flux sync:
#
#   kubectl -n claude-queue create secret generic claude-queue-secrets \
#     --from-literal=JWT_SECRET=$(openssl rand -hex 32) \
#     --from-literal=CLAUDE_KEY_ENCRYPTION_SECRET=$(openssl rand -hex 32) \
#     --dry-run=client -o yaml \
#   | kubeseal --format yaml > k8s/01-secrets-sealed.yaml
#
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: claude-queue-secrets
  namespace: claude-queue
spec:
  encryptedData:
    JWT_SECRET: REPLACE_ME__generate-with-kubeseal-see-header
    CLAUDE_KEY_ENCRYPTION_SECRET: REPLACE_ME__generate-with-kubeseal-see-header
  template:
    metadata:
      name: claude-queue-secrets
      namespace: claude-queue
